# 路径一致性审计

> 审计时间: 2026-05-04
> 扫描目录: 02_知识卡片, 03_原子问答, 05_知识关联, 06_RAG入库准备, 07_报告, 09_工作进度

## 审计规则

1. 长期 source 引用必须使用 `01_源文件库/...` 相对路径
2. 每个 source 引用必须同时有 `source_id`
3. 不允许使用 `~/Desktop/Eurocrypt 2027/...` 绝对路径
4. 不允许只写简写路径 (如 `域论/...`, `NTT/...`, `PaCo翻译§4`)
5. 未导入的外部论文/PDF 标记为 `source_pending_import`

## 扫描结果

- 扫描文件数: 21
- 发现不规范引用: 29

### 按类型分布

| 类型 | 数量 | 说明 |
|------|------|------|
| shorthand (简写路径) | 29 | 使用简写路径而非 01_源文件库/ 前缀 |
| temp_abs (临时绝对路径) | 0 | 包含 ~/Desktop/Eurocrypt 2027 |
| missing_source_id | 0 | 引用存在但缺少 source_id |
| pending_import | 0 | 引用未导入的外部文件 |

## 待导入文件

0 个文件被引用但未导入源文件库。
详见: `00_导入记录/缺失源文件清单.md`

## 详细引用审计

详见: `00_导入记录/source引用审计表.md`
