# Q155

**Q**: NTT的本质是什么——"找周期"还是"加速卷积"？

**A**: 两个用途都是DFT/NTT的不同侧面：信号处理用频谱分析（频域分解），密码学/算法用卷积定理（时域卷积↔频域点乘）。在FHE中我们关心后者：NTT将多项式乘法(O(n²))转化为求值(O(n log n))→点乘(O(n))→插值(O(n log n))，总复杂度O(n log n)。

card: K053 | source: src_000093