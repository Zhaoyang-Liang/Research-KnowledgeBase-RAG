# Q143

**Q**: C2S/S2C在CKKS bootstrapping管线中处于哪个位置？

**A**: 管线：ModRaise→C2S→EvalMod→S2C→ModSwitch。C2S在模数提升后、同态求值前（将slot表示转为系数表示供EvalMod使用）。S2C在求值后、模数降低前（将结果转回slot表示）。

card: K049 | source: synthesized