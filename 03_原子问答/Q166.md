# Q166

**Q**: 密钥切换密钥(KSK)是什么？为什么需要预计算？

**A**: KSK={Enc_{s'}(s·g^i)}是公钥的一部分，其中g是分解基（如2^L，用于将s分解为小系数片段以减少切换噪声）。需要预计算因为：(1)KSK的生成需要知道s（私钥），只能由私钥持有者在Setup阶段生成；(2)同态运算时可以直接使用预计算的KSK，不需在线访问私钥。

card: K059 | source: src_000104