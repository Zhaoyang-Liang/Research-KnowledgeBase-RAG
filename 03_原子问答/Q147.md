# Q147

**Q**: SIMD编码中解码为什么必须基于Φ_M的根，不能随便取点？

**A**: 因为解码映射a(X)↦(a(α₀),...,a(α_{L-1}))必须是环同态才能保持乘法结构。只有α_i取自不可约因子的零点时，求值映射在商环R_t上良定义。若取任意点，a·b在该点的值≠a在该点值·b在该点值（模Φ_M不保证），导致解码后乘法不正确。

card: K050 | source: src_000087