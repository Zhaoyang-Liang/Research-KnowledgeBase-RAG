# Q153

**Q**: Rotation操作和C2S/S2C有什么关系？

**A**: 不同概念。Rotation是使用自同构密钥在密文上执行槽之间的循环位移（置换操作），使用τ_j原语。C2S/S2C是将同一密文在slot表示和coefficient表示之间转换（线性变换），使用矩阵分解/CT算法。两者使用不同同态原语、不同目标、不同复杂度模型。

card: K052 | source: src_000083,src_000084