# 全局术语公式索引

**术语**: 8 | **公式**: 10

## 术语

1. **Product操作 Pr_{n→B}** [Batch-03] — CKKS同态操作，将槽向量分组后对组内元素做Hadamard乘积。是数域扩张中范数(Norm)在槽表示下的类比。消耗对数级别。
2. **PaCo结构化密钥** [Batch-03] — h组×4条候选支路，每组通过selector d_α恰好激活1条，B=N/(4h)。密钥s=Σd_α·X^{u_α·4h+α}，其中u_α∈[0,B)随机采样。解密式可压缩为ΣX^{u_{λ_j}·4
3. **split-compatible sandwich circuit** [Batch-03] — 作者提出的抽象框架：形式为L^(2)∘Φ∘L^(1)的同态电路，其中Φ是逐坐标非线性函数。若L^(1)和L^(2)满足split-compatible条件，则整个电路可分解为⊕leaf的形式。CKKS
4. **5 mod 4n的乘法阶** [Batch-03] — 当n=2^k时，5 mod 4n=2^{k+2}的乘法阶为n=2^k。因此{5^i mod 4n: i=0,...,n-1}给出n个互异的模4余1的奇数，对应所有模4余1的与4n互素的数。这是CKKS
5. **Trace操作 Tr_{n→B}** [Batch-03] — CKKS同态操作，将长度为n的槽向量按组"水平求和"压缩为长度B的向量。每组n/B个槽相加。仅用旋转+加法实现，不消耗乘法级别。用于内积计算和自举中的系数折叠。
6. **SlotToCoeff (密文态语义)** [Batch-03] — 不是"解密→重编码"，而是通过同态线性变换将一个槽向量语义密文转换为多项式系数语义密文的表示切换操作。在CKKS中通过同态加法和Vandermonde矩阵-密文乘法实现，全程无解密。
7. **C^B嵌入C^n (ι)** [Batch-03] — ι: C^B→C^n定义为ι(z)=(z,...,z)重复M=n/B次。ι是Hadamard积下的环同态，C^B的像构成C^n的子环。关键性质：Ecd_n∘ι=Ecd_B，即编码在嵌入下保持一致。
8. **典范嵌入 τ_n** [Batch-03] — τ_n: R[Y]/(Y^{2n}+1)→C^n，定义为τ_n(p)=(p(ζ_{n,i}))_{i∈[0,n)}，其中ζ_{n,i}=exp(2πi·5^i/(4n))。正向=多项式在n个复点上的求

## 公式

1. **C^B嵌入编码保持** [Batch-03] — \operatorname{Ecd}_n(\iota(\mathbf{z}_1))=\operatorname{Ecd}_B(\mathbf{z}_1),\quad \iota(\mathbf{z}_
2. **5的阶: n=2^k** [Batch-03] — \operatorname{ord}_{4n}(5)=n=2^k,\quad \{5^i\bmod 4n\}_{i=0}^{n-1}=\{k\mid \gcd(k,4n)=1,\ k\equiv 1\
3. **split-compatible criterion** [Batch-03] — F_N=\operatorname{Merge}_k\circ\left(\bigoplus_r L_n^{(2)}\circ\Phi_n\circ L_n^{(1)}\right)\circ\ope
4. **γ_impl定义** [Batch-03] — \gamma_{\mathrm{impl}}=\frac{\operatorname{C2SScaling}_{\mathrm{leaf}}\cdot\operatorname{S2CScaling}
5. **τ_n正向公式** [Batch-03] — \tau_n(p(Y))=(p(\zeta_{n,i}))_{i\in[0,n)}=U_n(\mathbf{p}_0+\mathbf{I}\mathbf{p}_1)
6. **Trace操作定义** [Batch-03] — \mathsf{Tr}_{n\to B}(\mathbf{z})_i=\sum_{j=0}^{n/B-1}z_{i+jB}
7. **SlotToCoeff密文层公式** [Batch-03] — \mathbf{ct}_0=\mathsf{Enc}^*(\mathbf{p}_0),\ \mathbf{ct}_1=\mathsf{Enc}^*(\mathbf{p}_1),\ \mathbf{ct
8. **PaCo解密压缩式** [Batch-03] — m=\sum_{\alpha=0}^{4h-1}d_\alpha X^{u_\alpha 4h}a_\alpha=\sum_{j=0}^{h-1}X^{u_{\lambda_j}4h}a_{\lamb
9. **主定理框架** [Batch-03] — \widetilde{\operatorname{BTS}}_{N,k}^{\mathrm{fac}}=\gamma_{\mathrm{impl}}^{-1}\operatorname{Merge}_
10. **τ_n^{-1}完整公式** [Batch-03] — \mathbf{w}=U_n^{-1}\mathbf{z},\ \mathbf{w}=\mathbf{p}_0+\mathbf{I}\mathbf{p}_1,\ p(Y)=\sum p_jY^j+\s
